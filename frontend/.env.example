# Firebase 설정
# Firebase Console (https://console.firebase.google.com)에서
# 프로젝트 설정 → 웹 앱 → SDK 설정 및 구성에서 값을 복사하세요

VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# Naver OAuth (선택사항)
VITE_NAVER_CLIENT_ID=
# Optional: override redirect, defaults to `${window.location.origin}/auth/naver/callback`
VITE_NAVER_REDIRECT_URI=

# Firebase Emulator 설정 (로컬 개발용)
# true로 설정하면 Firebase Emulator 사용
VITE_USE_FIREBASE_EMULATOR=false
VITE_FUNCTIONS_EMULATOR_HOST=localhost:5001
VITE_FIRESTORE_EMULATOR_HOST=localhost:8080
VITE_AUTH_EMULATOR_HOST=localhost:9099

# ⚠️ 중요: .env 파일은 Git에 커밋하지 마세요!
# .gitignore에 이미 추가되어 있습니다.
